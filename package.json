{
  "name": "json-server-api",
  "version": "1.0.0",
  "description": "A simple JSON server",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "json-server --watch db.json --port 8080"
  },
  "dependencies": {
    "json-server": "^0.17.0"
  },
  "devDependencies": {}
}
